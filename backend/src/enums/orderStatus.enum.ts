export enum OrderStatus {
  PENDING = "pending",
  PAID = "paid",
  SHIPPED = "shipped",
  DELIVERED = "delivered",
  CANCELLED = "cancelled",
}

export enum PaymentType {
  CLICK = "click",
  PAYME = "payme",
  CARD = "card",
  CASH = "cash",
}
